name: Notify nimpress docs
description: Trigger the docs site repo to sync this repo, from its export folder or from pages marked with the export frontmatter header
inputs:
  docs-repo:
    description: Full name of the docs site repo
    required: true
  token:
    description: Token with dispatch access to the docs site repo
    required: true
  event-type:
    description: Repository dispatch event type
    required: false
    default: nimpress-docs-sync
  content-dir:
    description: Content root scanned for export marked pages when the export folder is absent
    required: false
    default: docs
  export-dir:
    description: Folder holding the pages to publish, relative to the repo root
    required: false
    default: .nimpress
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version: '1.22'
    - shell: bash
      working-directory: ${{ github.action_path }}
      env:
        GITHUB_TOKEN: ${{ inputs.token }}
        NIMPRESS_DOCS_REPO: ${{ inputs.docs-repo }}
        NIMPRESS_EVENT_TYPE: ${{ inputs.event-type }}
        NIMPRESS_CONTENT_DIR: ${{ inputs.content-dir }}
        NIMPRESS_EXPORT_DIR: ${{ inputs.export-dir }}
        NIMPRESS_SOURCE_REPO: ${{ github.repository }}
        NIMPRESS_SHA: ${{ github.sha }}
        NIMPRESS_REF: ${{ github.ref_name }}
        NIMPRESS_WORKSPACE: ${{ github.workspace }}
      run: go run .
