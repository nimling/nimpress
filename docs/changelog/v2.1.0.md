---
title: Changelog
type: changelog
data:
  version: 2.1.0
  release_date: 2026-07-26
  title: Sidebar icons and a steadier rail
  description: Sidebar rows take an icon as text or artwork and the page rail keeps its place without a mirror column.
---

Sidebar rows carry real artwork, the root page can join the sidebar as a top level item, and the page rail holds its position through container queries.

## Icons on sidebar rows

An `icon` on a `sidebar` block or on a story definition accepts three kinds of value.

1. Literal text or ascii art, rendered verbatim in the mono font.

2. Inline `<svg>` markup, rendered as markup.

3. A path ending in `.svg`, resolved against the declaring file or against the content root when it starts with a slash, and inlined at build time.

An unreadable path warns and drops the icon. Icons render from `sidebar` blocks and story definitions only.

## The root page in the sidebar

The root index joins the sidebar through its own `sidebar` block placed at `sidebar.path`, so the site landing page becomes a clickable top level item rather than an entry with no row.

## Page rail

The right rail switches to container queries on the page shell and the mirror column beside it is gone. The rail holds its place as the shell resizes instead of reacting to the viewport.

## Story rows

A story decorates its own sidebar row through a `sidebar` object carrying `name`, `icon`, and `style`. The link stays keyed on the resolved story name, so only the label changes and the URL does not.

An underscore in a story file name reads as a space when the name falls back to the file name, so a file named for a case renders a readable row.

## Dev rebuilds

Page bodies and page component modules invalidate with their registered ids, so editing a story file rebuilds the component data behind the page it belongs to.
