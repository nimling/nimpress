---
title: Changelog
type: changelog
data:
  version: 1.6.0
  release_date: 2026-07-16
  title: Page visibility and a composable harness
  description: Pages declare where they appear through one visibility field and a component harness composes from primitives.
---

Where a page appears is now one field with three states. The component harness stops being a fixed frame and becomes a set of primitives a library arranges itself.

## Page visibility

**Breaking.** A page declares where it appears through `visibility`, which takes `visible`, `hidden`, or `dev-only`. The boolean flag that carried this before is gone from the schema.

1. `visible` is the default and needs no declaration.

2. `hidden` takes the page out of the sidebar, out of search, and out of the build.

3. `dev-only` keeps the page in `nimpress dev` and leaves it out of the built bundle. Its sidebar row carries a red dot so the author can see at a glance that the page does not ship.

The frontmatter schema accepts unknown fields without failing, so a page still carrying the boolean builds and publishes as a normal public page. Any page meant to stay unpublished has to be moved to `visibility` to keep its intent.

## Composable harness

A component system declares its frame instead of relying on discovery. `ComponentHarness`, `ComponentStory`, `ComponentHarnessEffects`, and `ComponentHarnessOverlay` import from the harness package and arrange freely, and a system that declares no harness gets the default composition.

1. The `harness` field on a system points at the component wrapping every story.

2. The `harness` field on a single story wraps that one story inside the system harness.

3. The `setup` module bootstraps the app, installing plugins and providers on every created app before mount.

## Sidebar groups from frontmatter

A `sidebar` block nests a page under a named group without changing its URL. A declared group sits beside the physical folders rather than inside one. On a folder index the block decorates that folder's own row.

## Guarded bundles

Guarded pages build into their own folders keyed by the bundle the guard function names, and the build writes the map of page to bundle alongside them.

## Workshop panels

The props panel and the console panel each dock to the bottom or the right of the workshop and split the slot when they share it. The console streams everything the component frame logs, including window errors, and takes typed expressions that evaluate inside the frame.

1. Events render as control rows with an always visible editor holding a real handler function.

2. A handler compiles in the frame and counts its firings on the row.

## Page rail

The right rail lives in a grid gutter on the page shell, so the content column stays centered and the rail stops clipping at narrow widths.

## Diagrams

Mermaid moves into the dependencies of the package, so a consumer resolves it through nimpress rather than installing it separately.
