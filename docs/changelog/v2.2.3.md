---
title: Changelog
type: changelog
data:
  version: 2.2.3
  release_date: 2026-08-13
  title: Login issuer on the relying party
  description: A site declares the issuer it logs in against and the session layer carries it through every call.
---

A site that logs readers in states which identity provider it trusts.

## The issuer travels with the request

The auth configuration carries the issuer, and the session layer passes it on every discovery, token, and userinfo call. A deployment pointed at a second provider changes one field instead of overriding each endpoint by hand.
