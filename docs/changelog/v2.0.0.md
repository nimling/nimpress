---
title: Changelog
type: changelog
data:
  version: 2.0.0
  release_date: 2026-07-22
  title: One paths config
  description: Every folder nimpress writes is declared in one place and the caches move under a single root.
---

Every folder the tool writes is declared in one optional `paths` block, each field carrying a default that keeps existing URLs unchanged.

## The paths block

1. `paths.out` is the build root and defaults to `dist`.

2. `paths.cache` is the single cache root and defaults to `node_modules/.nimpress`. The docs build cache, the harness cache for each component system, and the lint verification build all layer beneath it.

3. `paths.export` is the folder the export command fills for the docs pipeline and defaults to `.nimpress`.

4. `paths.modules` and `paths.guarded` are both the folders under the build root and the URL segments they are served at, defaulting to `_components` and `_guarded`.

## Breaking change

**Breaking.** `outDir` folds into `paths.out`. A config still setting `outDir` needs it moved.

## Lint leaves the repo root

The lint verification build writes under the cache root rather than the repo root, and cleans up after itself when the run is interrupted.

## Warnings stop repeating

Schema coaching warnings collect into one registry keyed by position, deduplicate, and flush once, grouped per component with the source file and a best effort line and a single suggested fix. They print after the dev banner, after each rebuild, and at the end of an update, an import, and a lint.

The automatic schema upsert in dev is guarded by a hash of the source content, so writing a schema no longer retriggers the pass that wrote it. The rebuild loop and the warning flood it produced are gone.
